/* Stand-in for miniz's build-system-generated export header: we compile the
 * decompressor into one translation unit, nothing is exported anywhere. */
#ifndef MINIZ_EXPORT_H
#define MINIZ_EXPORT_H
#define MINIZ_EXPORT
#endif
